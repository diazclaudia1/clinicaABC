version: '3'

services:

  nginx:
    image: nginx
    ports:
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - ./nginx/index.html:/app/html:ro
  

  reportes:
    build: reportes
    volumes:
      - ./reportes:/app

  reportesdos:
    build: reportesdos
    volumes:
      - ./reportesdos:/app

  auth:
    build: auth
    volumes:
      - ./auth:/app
    ports:
      - 5010:5000

